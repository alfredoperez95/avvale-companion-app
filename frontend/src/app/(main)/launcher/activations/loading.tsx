import { LoadingScreen } from '@/components/LoadingScreen/LoadingScreen';

export default function ActivationsLoading() {
  return <LoadingScreen message="Cargando activaciones..." fullPage={false} />;
}
