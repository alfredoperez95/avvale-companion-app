import { LoadingScreen } from '@/components/LoadingScreen/LoadingScreen';

export default function LauncherLoading() {
  return <LoadingScreen message="Abriendo App Launcher..." fullPage={false} />;
}
